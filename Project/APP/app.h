/**
 * @file app.h
 * @brief 应用程序全局配置头文件
 *
 * 本头文件包含应用程序的全局配置和管理接口，包括：
 * - 版本信息
 * - WiFi网络配置
 * - 主循环接口
 * - 线程安全接口
 * - 状态查询接口
 *
 * @author Smart Weather Clock Team
 * @version 1.0.0
 */

#ifndef __APP_H__
#define __APP_H__

/*============================================================================*/
/*                             版本配置                                       */
/*============================================================================*/

#define APP_VERSION "v1.0"    /**< 应用程序版本号 */

/*============================================================================*/
/*                             WiFi配置                                       */
/*============================================================================*/

#define WIFI_SSID   "iQOO Neo8 Pro"      /**< WiFi网络名称 */
#define WIFI_PASSWD "lhz19719937532"     /**< WiFi密码 */

/*============================================================================*/
/*                             WiFi接口函数                                   */
/*============================================================================*/

/**
 * @brief WiFi硬件及协议栈初始化
 * @return 1: 成功  0: 失败
 */
uint8_t wifi_init(void);

/**
 * @brief 发起WiFi连接并等待完成（阻塞式）
 */
void wifi_wait_connect(void);

/*============================================================================*/
/*                             主循环接口                                     */
/*============================================================================*/

/**
 * @brief 主循环初始化
 */
void main_loop_init(void);

/**
 * @brief 主循环执行函数
 */
void main_loop(void);

/*============================================================================*/
/*                             线程安全接口                                   */
/*============================================================================*/

/**
 * @brief 获取LCD互斥锁
 */
void lcd_lock(void);

/**
 * @brief 释放LCD互斥锁
 */
void lcd_unlock(void);

/**
 * @brief 获取ESP模块互斥锁
 */
void esp_lock(void);

/**
 * @brief 释放ESP模块互斥锁
 */
void esp_unlock(void);

/*============================================================================*/
/*                             状态查询接口                                   */
/*============================================================================*/

/**
 * @brief 检查WiFi是否已连接
 * @return 1: 已连接  0: 未连接
 */
uint8_t wifi_is_ready(void);

/**
 * @brief 检查时间是否已同步
 * @return 1: 已同步  0: 未同步
 */
uint8_t time_is_synced(void);

/**
 * @brief 检查主页面是否就绪
 * @return 1: 就绪  0: 未就绪
 */
uint8_t main_page_is_ready(void);

#endif /* __APP_H__ */
